# Secrets and credentials only. Non-secret settings belong in config.toml.
# Replace locally; never send real credentials to public demonstration services.
# Process environment variables override this file. Values are literal, not shell code.
API_TOKEN=replace-me
